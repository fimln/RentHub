name: renthub
description: RentHub - Platform Agregator Penyewaan Kendaraan Cerdas
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.1.1
  flutter_secure_storage: ^9.0.0
  socket_io_client: ^2.0.3+1
  google_fonts: ^8.0.0
  intl: ^0.18.1

  flutter_map: any
  latlong2: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
